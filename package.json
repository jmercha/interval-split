{
  "name": "split-interval",
  "version": "1.0.4",
  "description": "Splits a given interval across start and end times",
  "main": "dist/index.js",
  "types": "index.d.ts",
  "repository": "https://github.com/jmercha/split-interval",
  "author": "John Merchant <john@merchant.on.net>",
  "license": "MIT",
  "keywords": [
    "date",
    "time",
    "split",
    "interval"
  ],
  "scripts": {
    "test": "yarn jest",
    "build": "yarn babel index.js -d dist/"
  },
  "jest": {
    "testPathIgnorePatterns": [
      "dist"
    ]
  },
  "dependencies": {
    "@babel/runtime": "^7.5.4",
    "moment": "^2.24.0"
  },
  "devDependencies": {
    "@babel/cli": "^7.5.0",
    "@babel/core": "^7.5.4",
    "@babel/plugin-transform-runtime": "^7.5.0",
    "@babel/preset-env": "^7.5.4",
    "jest": "^24.8.0"
  }
}
